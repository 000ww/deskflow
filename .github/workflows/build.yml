# Deskflow Multi-Platform Build + Release

name: Build

on:
  push:
    branches: [ main, master ]
    tags: [ "v*" ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  QT_VERSION_LINUX: "6.7.2"
  QT_VERSION: "6.7.0"

jobs:
  # ============================================================
  # Windows x64
  # ============================================================
  build-windows-x64:
    name: Windows x64
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Qt using aqtinstall
        run: |
          pip install aqtinstall
          aqt install-qt windows desktop ${{ env.QT_VERSION }} win64_msvc2019_64 --outputdir C:\Qt
          echo "QT_ROOT_DIR=C:\Qt\${{ env.QT_VERSION }}\msvc2019_64" >> $env:GITHUB_ENV
          echo "Qt6_DIR=C:\Qt\${{ env.QT_VERSION }}\msvc2019_64\lib\cmake\Qt6" >> $env:GITHUB_ENV
        shell: pwsh

      - name: Install dependencies via Chocolatey
        run: |
          choco install ninja -y
          choco install openssl -y
          dotnet tool install --global wix --version 4.0.5
          echo "$env:USERPROFILE\.dotnet\tools" >> $env:GITHUB_PATH
          wix extension add WixToolset.Util.wixext
          wix extension add WixToolset.Firewall.wixext
        shell: pwsh

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure
        run: |
          $opensslPath = "C:\Program Files\OpenSSL"
          if (Test-Path $opensslPath) {
            Write-Host "Found OpenSSL at: $opensslPath"
            $env:OPENSSL_ROOT_DIR = $opensslPath
          }
          cmake -S . -B build `
            -G "Visual Studio 17 2022" `
            -A x64 `
            -DCMAKE_PREFIX_PATH="$env:QT_ROOT_DIR" `
            -DQt6_DIR="$env:Qt6_DIR" `
            -DOPENSSL_ROOT_DIR="$opensslPath" `
            -DBUILD_TESTS=OFF `
            -DBUILD_INSTALLER=ON
        shell: pwsh

      - name: Build
        run: cmake --build build --config Release -j $env:NUMBER_OF_PROCESSORS
        shell: pwsh

      - name: Find and copy OpenSSL DLLs
        run: |
          $opensslDlls = @(
            "C:\Program Files\OpenSSL\bin\libcrypto-3-x64.dll",
            "C:\Program Files\OpenSSL\bin\libssl-3-x64.dll",
            "C:\Program Files\OpenSSL-Win64\bin\libcrypto-3-x64.dll",
            "C:\Program Files\OpenSSL-Win64\bin\libssl-3-x64.dll"
          )
          $releaseDirs = @(
            "build\bin\Release",
            "build\src\apps\deskflow-core\Release",
            "build\src\apps\deskflow-daemon\Release"
          )
          foreach ($dll in $opensslDlls) {
            if (Test-Path $dll) {
              Write-Host "Found OpenSSL DLL: $dll"
              foreach ($dir in $releaseDirs) {
                if (Test-Path $dir) {
                  Copy-Item $dll $dir
                  Write-Host "Copied to $dir"
                }
              }
              break
            }
          }
        shell: pwsh

      - name: Create 7Z package
        run: cpack -G 7Z -C Release
        shell: pwsh
        working-directory: build

      - name: Create WiX installer
        id: wix
        run: cpack -G WIX -C Release
        shell: pwsh
        working-directory: build
        continue-on-error: true

      - name: List artifacts
        if: always()
        run: |
          Get-ChildItem -Path build -Include "*.7z","*.msi" -Recurse | ForEach-Object {
            Write-Host "$($_.FullName) ($([math]::Round($_.Length/1MB, 2)) MB)"
          }
        shell: pwsh

      - name: Upload 7Z package
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deskflow-windows-x64-portable
          path: build/*.7z

      - name: Upload WiX installer
        if: steps.wix.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: deskflow-windows-x64-installer
          path: build/*.msi

  # ============================================================
  # macOS
  # ============================================================
  build-macos:
    name: macOS
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          brew install openssl cmake ninja

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          host: mac
          target: desktop
          arch: clang_64
          dir: ${{ runner.temp }}/qt
          set-env: true

      - name: Configure
        run: |
          cmake -S . -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$Qt6_DIR" \
            -DQt6_DIR="$Qt6_DIR" \
            -DOPENSSL_ROOT_DIR="$(brew --prefix openssl@3)" \
            -DBUILD_TESTS=OFF \
            -DBUILD_INSTALLER=ON

      - name: Build
        run: cmake --build build -j $(sysctl -n hw.ncpu)

      - name: Create DMG
        run: cpack -G DragNDrop -C Release
        working-directory: build

      - name: List artifacts
        if: always()
        run: ls -lh build/*.dmg 2>/dev/null || echo "No DMG found"

      - name: Upload DMG
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deskflow-macos-dmg
          path: build/*.dmg

  # ============================================================
  # Linux x64 (Ubuntu)
  # ============================================================
  build-linux:
    name: Linux x64
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo add-apt-repository -y ppa:lopin/deskflow
          sudo apt-get update
          sudo apt-get install -y \
            cmake ninja-build g++ \
            libssl-dev \
            libgl1-mesa-dev \
            libx11-dev libxtst-dev libxkbfile-dev \
            libxrandr-dev libxinerama-dev libxi-dev \
            libxext-dev libice-dev libsm-dev \
            libglib2.0-dev libxkbcommon-dev \
            libei-dev libportal-dev \
            pkg-config \
            libegl1 libopengl0

      - name: Install Qt via aqtinstall
        run: |
          pip install aqtinstall
          aqt install-qt linux desktop ${{ env.QT_VERSION_LINUX }} linux_gcc_64 --outputdir /opt/qt
          echo "QT_ROOT_DIR=/opt/qt/${{ env.QT_VERSION_LINUX }}/gcc_64" >> $GITHUB_ENV
          echo "Qt6_DIR=/opt/qt/${{ env.QT_VERSION_LINUX }}/gcc_64/lib/cmake/Qt6" >> $GITHUB_ENV
          echo "/opt/qt/${{ env.QT_VERSION_LINUX }}/gcc_64/bin" >> $GITHUB_PATH

      - name: Configure
        run: |
          cmake -S . -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_FLAGS="-fpermissive" \
            -DCMAKE_PREFIX_PATH="$QT_ROOT_DIR" \
            -DQt6_DIR="$Qt6_DIR" \
            -DBUILD_TESTS=OFF \
            -DBUILD_INSTALLER=ON

      - name: Build
        run: cmake --build build -j $(nproc)

      - name: Create packages
        run: |
          cpack -G TGZ -C Release
          cpack -G DEB -C Release || echo "DEB packaging failed, continuing with TGZ"
        working-directory: build

      - name: List artifacts
        if: always()
        run: ls -lh build/*.tar.gz build/*.deb 2>/dev/null || echo "No packages found"

      - name: Upload Linux packages
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deskflow-linux-packages
          path: |
            build/*.tar.gz
            build/*.deb

  # ============================================================
  # Create GitHub Release (only on version tag push)
  # ============================================================
  release:
    name: Create Release
    needs: [ build-windows-x64, build-macos ]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List downloaded artifacts
        run: find artifacts -type f | head -50

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Deskflow ${{ github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') || contains(github.ref_name, 'rc') }}
          generate_release_notes: true
          files: |
            artifacts/**/*.7z
            artifacts/**/*.msi
            artifacts/**/*.dmg
            artifacts/**/*.deb
            artifacts/**/*.tar.gz
